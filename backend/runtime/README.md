Runtime-generated files live here.

- `database/`: SQLite database file created by the app at runtime
- `reports/`: generated PDF and CSV reports
- `uploads/`: temporary and saved uploaded media files

This keeps generated data separate from source code and makes cleanup safer.
